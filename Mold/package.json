{
  "name": "mold",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write --plugin-search-dir=. ."
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
